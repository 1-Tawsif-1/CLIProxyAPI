services:
  - type: web
    name: cliproxyapi
    runtime: docker
    plan: free
    healthCheckPath: /v1/models
    envVars:
      - key: DEPLOY
        value: cloud
      - key: CODEX_TOKEN_JSON
        sync: false
