services:
  - type: web
    name: cliproxyapi
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: DEPLOY
        value: cloud
      - key: CODEX_TOKEN_JSON
        sync: false
